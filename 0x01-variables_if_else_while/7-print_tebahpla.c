#include <stdio.h>

/**
 * main - the entry point of a program
 * Return: 0 shows the successful of a program
 */
int main(void)
{
	char i;

	for (i = 'z'; i >= 'a'; i--)
		putchar (i);
	{
		putchar('\n');
	}
	return (0);
}
