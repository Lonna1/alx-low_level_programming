#include <stdio.h>

/**
 * main - The entry point of a program
 * Return: 0 shows the success of the program
 */

int main(void)
{
	char b;

	for (b = 'a'; b <= 'z'; b++)
		putchar(b);
	{
		putchar("\n");
	}
	return (0);
}
