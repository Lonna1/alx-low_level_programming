#include <stdio.h>

/**
 * main - Entry point of a program
 * Return: 0 shows the success of the program
 */

int main(void)

{

	char xy;

	for (xy = 'a'; xy <= 'z'; xy++)

	{
		putchar(xy);

	}

	putchar('\n');

	return (0);

}
