#include <stdio.h>

/**
 * main - The entry point of a program
 * Return: 0 shows the success of a program
 */

int main(void)
{
	int b;

	for (b = 48; b < 58; b++)
	putchar(b);
	{
		putchar("\n")
	}
	return (0);
}
