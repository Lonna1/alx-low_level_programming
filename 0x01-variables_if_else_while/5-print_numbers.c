#include <stdio.h>

/**
 * main - This is the entry point of the program
 * Return: 0 shows the successful of a program
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	return (0);
}

