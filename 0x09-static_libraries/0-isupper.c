#include "main.h"

/**
 * _isupper - checks for uppercase character in the program
 * @letter: the character to be checked
 * Return: 1 if c is uppercase, 0 if it is not
 */
int _isupper(int letter)
{
	return (letter >= 'A' && letter <= 'Z');
}
