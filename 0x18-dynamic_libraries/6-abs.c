#include "main.h"

/**
 * _abs - helps to get the absolute value of an integer
 * @x: helps to check the absolute value
 * Return: help to check the successfullness of a program
 */
int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}

}
