#include <stdio.h>

/**
 * _abs - This will help in computing the absolute value of a number
 * Return : n shows the successful of a program
 * Return : -n shows the successful of a program
 */

int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	else(x >0)
	{
		return (x);
	}

}
